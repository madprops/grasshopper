`git clone` the repo.

Use `./utils/bundle.rb` when the js files change.

This is because all the js files are bundled into 2 main files.

Load the extension in `about:debugging`.

Loading the manifest file works.

On changes use `Reload` in `about:debugging`.

If in sidebar mode use the `Restart` command, or toggle the sidebar.
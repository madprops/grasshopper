#!/usr/bin/env ruby

# Main function
def main()
  name = ARGV[0]
  version = ARGV[1]
  zip_name = "#{name}_v#{version}.zip"
  `zip #{zip_name} -x \*.zip -r \*`
  puts "Created #{zip_name}"
end

# Start here
if __FILE__ == $0
  main()
end